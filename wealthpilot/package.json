{
  "name": "wealthpilot",
  "version": "1.1.0",
  "private": true,
  "description": "知衡 WealthPilot — 可解释的 AI 理财研究与模拟投资工作台",
  "type": "module",
  "engines": { "node": ">=24.0.0" },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "dev": "node --watch --env-file-if-exists=.env server.mjs",
    "test": "node --test",
    "build": "node scripts/check.mjs"
  },
  "license": "MIT"
}
